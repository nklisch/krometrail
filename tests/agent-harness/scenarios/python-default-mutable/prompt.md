The `daily_report` function in `sales.py` is generating incorrect reports. Each day's report should only count that day's sales, but the totals keep growing across days.

The test in `test_sales.py` demonstrates the failure. Debug this issue and fix the bug so that `test_sales.py` passes.
