{
  "mcpServers": {
    "krometrail": {
      "command": "${CLAUDE_PLUGIN_ROOT}/bin/krometrail",
      "args": [
        "mcp"
      ],
      "env": {
        "KROMETRAIL_MANAGED_ROOT": "${CLAUDE_PLUGIN_DATA}"
      }
    }
  }
}
